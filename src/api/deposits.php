<?php

namespace MidPay;

$orderId = Params::url(1);

if (Params::method() == 'PUT') {

	/* Todo: follow the deposits.txt instructions */


} else if (Params::method() == 'GET') {

	/* Todo: simply construct the response */

	/* currently, we only support outputing a single deposit */

}